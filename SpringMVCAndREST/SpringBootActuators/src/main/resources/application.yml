management:
  endpoints:
    web:
      exposure:
        exclude: health,mappings,beans
        include: '*'
  endpoint:
    shutdown:
      enabled: true
server:
  port: 5665
spring:
  application:
    name: actuator demo
